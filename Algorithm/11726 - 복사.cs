using System;

namespace Algorithm
{
    class Program
    {
        static void Main(string[] args)
        {
            int input = Convert.ToInt32(Console.ReadLine());

            double rangeMin = Math.Pow(10, input - 2);
            double rangeMax = Math.Pow(10, input - 1);


            for (double num = rangeMin; num < rangeMax; num++)
            {
                if()
            }
        }
    }
}
